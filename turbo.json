{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "env": ["LIASE_SECRETS_FILE", "GIPHY_API_KEY"]
    },
    "lint": {},
    "lint:fix": {}
  }
}
